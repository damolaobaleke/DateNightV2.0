<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.immersia_datenight">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity
            android:name=".views.unity.UnityEnvironmentLoad"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ExperienceType"
            android:label="@string/title_activity_experience_type"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".JoinDate" />
        <activity android:name=".CreateDate" />
        <activity
            android:name=".DateHubNavigation"
            android:label="@string/title_activity_create_date"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".BoardingDateScreen" />
        <activity android:name=".GetBitmojiActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".BoardingScreen" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>